
Цель: заново выдать доступ к админке для `argonaftmail@gmail.com` без лишних изменений в сайте.

Что сделаю после одобрения:
1. Найду текущий аккаунт пользователя по email `argonaftmail@gmail.com` в системе авторизации и проверю его актуальный `user_id`.
2. Проверю, есть ли у этого `user_id` роль `admin` в таблице `public.user_roles`.
3. Если роли нет — заново выдам её как обновление данных в backend, безопасно и без новой схемной миграции.
4. Если роль уже есть — не буду трогать код, а проверю следующую причину проблемы: статус аккаунта, пароль для входа или доступность маршрута после публикации.
5. После этого дам вам точную инструкцию входа: ссылка, какой email использовать и что нужен именно пароль от этого аккаунта.

Технические детали:
- Админка уже реализована в коде: маршруты `/admin` и `/admin/login` существуют.
- Доступ определяется не email напрямую, а записью в `public.user_roles` с ролью `admin`, привязанной к `user_id`.
- Ранее права уже выдавались для конкретного UUID. Если аккаунт пересоздавался, старый UUID мог измениться — тогда права нужно назначить повторно уже новому `user_id`.
- Для этой задачи, скорее всего, не потребуется менять фронтенд: проблема выглядит как повторная выдача прав или проверка текущего аккаунта.

Ожидаемый результат:
- `argonaftmail@gmail.com` снова получит доступ к админке и сможет входить на `/admin/login` под своим email и паролем.
